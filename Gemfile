source 'https://rubygems.org'

# Specify your gem's dependencies in super_awesome_print_rubocop.gemspec
gemspec

rubocop_version = ENV.fetch('RUBOCOP_VERSION', 'latest')
gem 'rubocop', rubocop_version unless rubocop_version == 'latest'
